#include "stdio.h"
int main(int argc, char const *argv[])
{
	double firstvarible;     // Declaration of variable that are strings 
	double secoundvarible;
	firstvarible = 255;
	secoundvarible = 355;
	printf("firstvarible=%.1f\n",firstvarible);
	secoundvarible=455;
	printf("secoundvarible=%.1f\n",secoundvarible);
	return 0;
}
//here this is one line comment
//double denotes a string variable and int denotes the integral varibles ??
//here the 1f %.1f denotes the value after the decimal point 
//the values of variable can be changed anywhere in the function
// we can denote two variables at once like double firstvarible,secoundvarible;
// the values can be alloted to the variables when they are been declared like for example double firstvariable=25;