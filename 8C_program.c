//this was an example for the costants and #define usage
#include <stdio.h>
int main(int argc, char const *argv[])
{
	
	#define Pi 3.14156
	printf("%.5f\n",Pi );
	return 0;
}
/* here in the #define funtion we do not need all those epual to and "" Marks so don't use them here and the value could be used by 
using the %.f value %d and %s notation its both the string and the variable at the same time */